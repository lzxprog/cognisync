# requirements.txt (由 conda environment.yml 转换)

### 核心依赖 ###
# --- 必须通过 conda 安装的包 (系统级依赖) ---
# 需先执行: conda install faiss-cpu numpy=1.23.5 pytorch=2.6.0

### 纯Python包 (可通过pip安装) ###
# --- 文件处理 ---
lxml==5.3.1
pymupdf==1.25.3
python-docx==1.1.2

# --- NLP/深度学习 ---
sentence-transformers==3.4.1
transformers==4.49.0
tokenizers==0.21.0
datasets==2.14.4

# --- Web服务 ---
fastapi==0.95.1
uvicorn==0.22.0
starlette==0.26.1

# --- 其他工具 ---
openai==0.27.0
python-dotenv==0.21.1
pandas==2.2.3
numpy==1.23.5

### 安装说明 ###
# 1. 先安装conda依赖:
#    conda install faiss-cpu pytorch=2.6.0 numpy=1.23.5
# 2. 再安装pip包:
#    pip install -r requirements.txt